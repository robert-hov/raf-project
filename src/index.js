import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.scss';

const rafo = ReactDOM.createRoot(document.getElementById('rafo'));
rafo.render(
    <App />
);
